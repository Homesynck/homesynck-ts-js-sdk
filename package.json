{
  "name": "homesynck-sdk",
  "version": "1.0.7",
  "description": "The easy asynchronous SDK for Homesynck",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist",
    "/browser"
  ],
  "scripts": {
    "start": "tsc && node dist/examples/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Homesynck team",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "phoenix-channels": "^1.0.0"
  },
  "devDependencies": {
    "tslint": "^5.12.1",
    "typescript": "^4.2.3"
  }
}
